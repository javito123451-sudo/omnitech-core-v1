- [Control Center Phase 1](control-center.md) — 9-section Control Center complete: Dashboard, Workspaces, Usuarios, Roles, Módulos, IA, Integraciones, Seguridad, Auditoría. Nav grouped in 3 sections. New routes: /roles, /integrations, /audit, /workspaces/:id. New backend: GET /workspaces/:id, GET /workspaces/:id/members, PATCH /workspaces/:id/members/:clerkId, GET /integrations, GET /security/summary.
- [Calendar premium upgrade](calendar-upgrade.md) — Calendar has 3 views (week/month/day), new statuses (pending/confirmed/completed/cancelled), new DB columns (reminder/tags/location), AI panel at /api/calendar-ai, drag-and-drop on week grid.
- [Quotes module](quotes-module.md) — Full quotes/presupuestos module: DB tables (quotes, quote_items), CRUD API at /api/quotes, PDF via pdfkit (external in esbuild), frontend at /quotes, AI tool create_quote in chat.ts.
- [Executive Intelligence Layer](executive-dashboard.md) — Dashboard at /executive: forecast, risks, priorities, opportunities. Babel backtick-in-template-literal gotcha documented.
- [API codegen workflow](codegen-workflow.md) — Orval codegen via `pnpm --filter @workspace/api-spec run codegen`; DB migration via `pnpm --filter @workspace/db exec drizzle-kit push --force`; pg available via psql "$DATABASE_URL" for direct SQL (node pg import hangs in bash).
- [Phase 1 multi-tenant auth](phase1-multitenant.md) — Clerk + org_id row-level isolation implemented; FK migration requires pre-seeding default org before drizzle-kit push.
- [WhatsApp automation flow](whatsapp-automation.md) — Full acceptance engine: webhook→keyword→quote accepted→client active→memory entry→auto-reply. Audit at GET /api/whatsapp/audit, page at /integrations/whatsapp/logs. DB schema changes: integration_events.payload_json, agent_memory.(title,category,tags,created_at) — added via direct SQL since drizzle-kit push not used.
- [Ava Autopilot scheduler design](ava-autopilot.md) — shouldRunTask is async (queries DB for condition triggers); nextRunAt advances on success only; in-flight run row is the idempotency lock.
- [Manual wiki module](manual-wiki.md) — Full wiki at /manual with sidebar, search, dark mode, versioning, edit (SUPER_ADMIN/STAFF_OMNITECH), 13 chapters seeded. DB: docs_pages, docs_versions. API: /api/docs (requireAuth, no resolveOrg needed). Frontend: ManualLayout + manual/index.tsx + manual/chapter.tsx.
- [Telegram CRM intent audit rules](telegram-crm-rules.md) — CRM-001/002/003 implemented: greeting-only filter, system-prompt appointment guard, DB readback validation. System prompt wording is critical — see topic file.
- [Timezone handling — Europe/Madrid](timezone-madrid.md) — madridLocalToUTC probe technique in telegram.ts, chat.ts, calendar.tsx. Store real UTC, display Madrid. Old "wall-clock as UTC" data is shifted +2h with new helpers.
- [Telegram config column JSON bug](telegram-config-bug.md) — org_integrations.config is TEXT (JSON string). Never spread it as object — always JSON.parse() first. RangeError when spreading string chars. Diagnostic page at /integrations/telegram/diagnostico.
- [Telegram Intelligent Assistant](telegram-intelligent-assistant.md) — 6-phase upgrade complete. Memory bug fixed: root cause was email NOT NULL constraint on clients table breaking Telegram auto-create → client=null → no history. Fixed via ALTER TABLE + 3 code bugs. Debug endpoint at GET /api/telegram/debug/:clientId.
- [Telegram clients table nullable](telegram-clients-nullable.md) — clients.email and clients.phone must be nullable for Telegram auto-create (no email/phone available from Telegram). Fixed via direct SQL ALTER TABLE.
- [AI Center + Omni Import AI](ai-center-import.md) — AI Center at /control-center/ai-center (SUPER_ADMIN only): logs every AI call, monthly budgets with 80/90/100% alerts, auto-block, financial dashboard. Omni Import AI at /import: GPT-4o vision for images, pdf-parse for PDF, xlsx for Excel; multer+pdf-parse+xlsx external in esbuild; use db.execute(sql`...`) NOT dynamic import("pg") — esbuild can't resolve it.
- [Omni Import AI — 3 root-cause fixes](import-ai-fixes.md) — max_tokens too low (2000→16000), Excel text truncated (8000 chars), PG UPDATE ORDER BY LIMIT invalid. pdf-parse v2 requires file path not buffer: write to tmpdir, parse, unlink.
- [Audit log system](audit-system.md) — Shared utility at utils/auditLogger.ts: logAudit(+req), logAuditSystem(no req), shouldLogLogin(8h dedup). All 6 fields captured. Login logged on GET /auth/me; logout on POST /auth/logout-event.
- [Omni Accounting Suite](omni-accounting.md) — Full accounting module: startup migrations FIX-D→FIX-H, API at /api/accounting (module-gated omni_accounting), 4-tab frontend (Dashboard/Facturas/Cobros/Contabilidad), client portal at /portal?token=, recurring invoices with 15-min scheduler.
- [Accounting startup scheduler ordering](accounting-scheduler-ordering.md) — Recurring invoice scheduler must start AFTER runStartupMigrations() completes (.finally()), not before — the recurring_invoices table is created in FIX-H.
- [Onboard Wizard](onboard-wizard.md) — 10-step wizard at /control-center/onboard-wizard (SUPER_ADMIN only) for creating fully-configured workspaces in <5 min. DB: onboard_wizard_drafts, onboard_templates (10 pre-seeded). API: GET/POST/PUT/DELETE drafts, GET templates, POST create (full workspace + license + modules + admin + team + clients + fiscal + integrations + AI config). Frontend: template selector + 10 steps with progress bar + summary + drafts save/load.
- [Omni Marketing Hub activation](marketing-hub-activation.md) — Full implementation: DB table `marketing_campaigns` (FIX-L migration), real CRUD API (POST/PATCH/DELETE/duplicate), audience from `clients` table, real analytics aggregated from DB. Frontend uses React Query, campaign modal, audience search/filter by segment, bar chart analytics. Permission bug fixed: was using non-existent `workspace.manage` → now `workspace.view`/`workspace.edit`.
- [SaaS audit fixes — TS + security](saas-audit-fixes.md) — dbRows<T> helper fixes TS2352 in diagnostic adapters; openaiProvider streaming cast to AsyncIterable; WhatsApp HMAC via rawBody in express.json verify + META_APP_SECRET; multer MIME whitelist in import-ai.
- [Production readiness state](env-production.md) — INTEGRATION_ENCRYPTION_KEY (64-char hex) and EMAIL_FROM set as shared env vars. RESEND_API_KEY deferred. META_APP_SECRET pending. Platform rated 7.2/10 — core CRM/billing/AI/channels ready, marketing sends nothing, no payment in portal, not deployed.
- [Public invoice share + recurring linkback](invoice-share-recurring.md) — Task #10: share_token on invoices (FIX-M), public route /api/accounting-public/invoices/:token, /invoice/:token frontend page. Task #11: invoices.recurring_invoice_id FK (FIX-N), scheduler populates on auto-generate.
- [db.execute() cast pattern](db-execute-cast.md) — Always use `function dbRows<T>(r: unknown): T[] { return (r as { rows: T[] }).rows; }` — direct cast causes TS2352 overlap error because QueryResult<Record<string,unknown>> doesn't overlap concrete row types.
- [OmniAds module](omni-ads.md) — Full AI ad platform at /ads (slug omni_ads). DB: ads_campaigns + ads_creatives (FIX-P). API: /api/ads (summary/campaigns CRUD/generate/creatives/audience/analytics). AI provider pattern: AdsAiProvider interface → getAdsAiProvider() → OpenAI GPT-4o; swap via registerAdsAiProvider(). Frontend: 5 tabs (Dashboard/Wizard/Creativos/Audiencia/Analíticas). Wizard: 5-step → POST campaign → POST :id/generate. Sidebar: Marketing group with Radio icon.
- [Creative Studio](creative-studio.md) — CreativesTab now fully functional: 6 clickable type cards (image/video/carousel/reel/story/email), CreativeGeneratorModal with type-specific fields, CreativeProgress animated steps, CreativeResult with 6 actions, CreativeHistory with type filters. AICreativeService singleton adapter pattern ready for OpenAI/Runway/Veo/etc. FIX-T adds 7 cols to ads_creatives. POST /api/ads/creatives/:id/generate stub.
- [Platform vs Workspace role refactor](platform-workspace-roles.md) — users.platform_role column added (FIX-O); useSuperAdmin reads from OrgContext not /check; platformRole in /api/auth/me response; new roles manager/client/guest in permissions.ts; "Rol CRM" → "Rol Workspace" in UI.
- [OmniLeads AI module](omnileads-module.md) — omni_leads at /leads (scale plan): Google Places search, AI analysis (11 signals+score), CRM integration, GPT-4o proposals. FIX-W tables. organizationsTable is in ./organizations not ./platform-admin; clients INSERT has no address/created_by.
- [Plan gating — known plans required](plan-gating.md) — planModules in auth.ts must include every real plan name used in DB. Unknown plan → fallback was starter (crm-only) → ALL modules set to false. Always add new plans; fallback is now ALL_MODULES (fail-open). Current DB plans: enterprise, professional. FIX-X bumps module versions on startup to clear stale sidebar cache.
- [Startup migration prod safety](startup-migration-prod-safety.md) — prod DB has different org IDs than dev. Any hardcoded org_id in startupMigrations.ts must guard: check org exists before INSERT (FK violation aborts entire chain). FIX-A must use OR name!='OmniTech Core' not just slug check. Prod orgs: 7 (omnitech-core/starter), 13 (a3servicios/free), 17, 21.
